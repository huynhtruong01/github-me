export enum Layout {
    DEFAULT = 'Mặc định',
    COMPACT = 'compact',
    DONUT = 'donut',
    DONUT_VERTICAL = 'donut-vertical',
    PIE = 'pie',
}

export enum RankIcon {
    DEFAULT = 'Mặc định',
    GITHUB = 'github',
    PERCENTILE = 'percentile',
}

export enum NumberFormat {
    SHORT = 'short',
    LONG = 'long',
}
